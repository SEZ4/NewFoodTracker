name: Weekly Reset

on:
  schedule:
    - cron: '21 0 * * 6'
  workflow_dispatch:

jobs:
  reset:
    runs-on: ubuntu-latest
    steps:
      - name: Call reset endpoint
        run: |
          curl -X POST https://new-food-tracker.vercel.app/reset-week 
  
